{
  "name": "hpw-pool-web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "node backend/server.cjs",
    "dev": "vite",
    "build": "echo 'No frontend build required'",
    "lint": "eslint .",
    "preview": "echo 'No preview in backend only mode'",
    "vercel-build": "echo 'Skipping Vite build - backend only'",
    "server": "node backend/server.cjs",
    "backend": "node backend/server.cjs"
  },
  "dependencies": { ... },
  "devDependencies": { ... }
}
